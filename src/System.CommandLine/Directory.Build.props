<Project>

  <PropertyGroup>
    <UseBetaVersion>true</UseBetaVersion>
  </PropertyGroup>

  <Import Project="Maid.props" />
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  
</Project>
